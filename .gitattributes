# Deterministic line endings so the Gradle wrapper + sources work on Linux CI
# regardless of a contributor's core.autocrlf setting.
* text=auto eol=lf

# Windows batch wrapper must stay CRLF
*.bat text eol=crlf

# Treat binaries as binary (no EOL munging)
*.jar binary
